@model IEnumerable<ContosoUniversity.Models.Courant>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Ajouter un courant littéraire", "Create")
</p>
<table class="table">
    <thead class="thead-dark">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.libelle_courant)
            </th>
            <th></th>
        </tr>
     </thead>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.libelle_courant)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.id_courant }, new { @class = "btn btn-success btn-sm" }) |
                    @Html.ActionLink("Details", "Details", new { id = item.id_courant }, new { @class = "btn btn-secondary btn-sm" }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.id_courant }, new { @class = "btn btn-danger btn-sm" })
                </td>
            </tr>
        }

</table>
